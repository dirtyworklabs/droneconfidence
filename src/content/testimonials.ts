import type { Testimonial } from '@/types'

/**
 * Real testimonials only.
 *
 * This array is intentionally empty. The testimonials section does not render
 * at all while it is empty — no sample quotes, no invented names, no star
 * ratings. Add genuine entries here and the section appears automatically.
 */
export const testimonials: Testimonial[] = []
